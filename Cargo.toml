[package]
name = "thanix_client"
version = "0.6.0"
authors = ["Christopher Hock <christopher-hock@protonmail.com>"]
edition = "2021"
description = "A netbox API client used as reference for the Nazara project. Generated from the schema of https://demo.netbox.dev"
readme = "README.md"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0.195", features = ["derive"] }
serde_qs = "0.12.0"
chrono = "0.4.31"
reqwest = { version = "0.11.22", features = ["blocking", "json"] }
